Soure code chay LIKE toán tử so khớp chuỗi (string matching) trong SQL.


Ưu điểm / Nhược điểm LIKE
✅ Ưu điểm

Rất đơn giản

Không cần model

Chạy nhanh với dữ liệu nhỏ

❌ Nhược điểm (RẤT QUAN TRỌNG)

❌ Không hiểu nghĩa

❌ Phụ thuộc đúng chữ

❌ Không chịu được lỗi chính tả

❌ Không dùng tốt cho chatbot 

Phiên bản chưa có embedding